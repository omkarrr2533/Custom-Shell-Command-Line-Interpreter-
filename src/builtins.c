#include "../include/builtins.h"

int shell_cd(char **args) {
    // Change directory
    return 0;
}

int shell_exit(char **args) {
    // Exit shell
    return 0;
}

int shell_help(char **args) {
    // Show help
    return 0;
}
